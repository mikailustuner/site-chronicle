{$SITECHRONICLE_HOST:localhost} {
  encode zstd gzip
  reverse_proxy api:43180
  header {
    -Server
    Strict-Transport-Security "max-age=31536000; includeSubDomains"
    X-Content-Type-Options "nosniff"
    Referrer-Policy "no-referrer"
  }
  log {
    output stdout
    format json
  }
}
